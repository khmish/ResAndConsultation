import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReviewAfterCommitteeRemarkComponent } from './review-after-committee-remark.component';

describe('ReviewAfterCommitteeRemarkComponent', () => {
  let component: ReviewAfterCommitteeRemarkComponent;
  let fixture: ComponentFixture<ReviewAfterCommitteeRemarkComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReviewAfterCommitteeRemarkComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReviewAfterCommitteeRemarkComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
