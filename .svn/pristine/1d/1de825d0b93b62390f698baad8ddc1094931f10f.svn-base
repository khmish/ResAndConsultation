import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DeputyRejectRemarkComponent } from './deputy-reject-remark.component';

describe('DeputyRejectRemarkComponent', () => {
  let component: DeputyRejectRemarkComponent;
  let fixture: ComponentFixture<DeputyRejectRemarkComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DeputyRejectRemarkComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DeputyRejectRemarkComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
