import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ConsultationFullDetailsComponent } from './consultation-full-details.component';

describe('ConsultationFullDetailsComponent', () => {
  let component: ConsultationFullDetailsComponent;
  let fixture: ComponentFixture<ConsultationFullDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ConsultationFullDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ConsultationFullDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
