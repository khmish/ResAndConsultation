import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ReviewResultAndFinalDecisionRemarksComponent } from './review-result-and-final-decision-remarks.component';

describe('ReviewResultAndFinalDecisionRemarksComponent', () => {
  let component: ReviewResultAndFinalDecisionRemarksComponent;
  let fixture: ComponentFixture<ReviewResultAndFinalDecisionRemarksComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ReviewResultAndFinalDecisionRemarksComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ReviewResultAndFinalDecisionRemarksComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
